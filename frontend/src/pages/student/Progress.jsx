import { useEffect, useState } from 'react';
const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:5000/api';
function Progress() { const user = JSON.parse(localStorage.getItem('user') || 'null'); const [items, setItems] = useState([]); useEffect(() => { if (user?.id) fetch(`${API_URL}/enrollments?student_id=${user.id}`).then((response) => response.json()).then(setItems); }, [user?.id]); const overall = items.length ? Math.round(items.reduce((total, item) => total + item.progress, 0) / items.length) : 0; return <main className="mx-auto max-w-5xl px-4 py-8 sm:px-6 lg:px-8"><h1 className="text-3xl font-extrabold text-slate-900">Progress</h1><div className="mt-6 rounded-2xl bg-primary-500 p-6 text-white"><p>Overall progress</p><p className="mt-2 text-5xl font-extrabold">{overall}%</p><div className="mt-4 h-2 rounded-full bg-white/30"><div className="h-2 rounded-full bg-white" style={{ width: `${overall}%` }} /></div></div><div className="mt-8 space-y-4">{items.map((item) => <div key={item.id} className="rounded-2xl border border-slate-200 bg-white p-5"><div className="flex justify-between gap-4"><h2 className="font-bold text-slate-900">{item.course?.title}</h2><span className="font-semibold text-primary-500">{item.progress}%</span></div><div className="mt-4 h-2 rounded-full bg-slate-100"><div className="h-2 rounded-full bg-primary-500" style={{ width: `${item.progress}%` }} /></div></div>)}</div></main>; }
export default Progress;
