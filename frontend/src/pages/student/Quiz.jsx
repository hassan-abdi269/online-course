import { useEffect, useState } from 'react';
import { useParams } from 'react-router-dom';
const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:5000/api';
function Quiz() { const { id } = useParams(); const [course, setCourse] = useState(null); const [selected, setSelected] = useState(''); const [submitted, setSubmitted] = useState(false); useEffect(() => { fetch(`${API_URL}/courses/${id}`).then((response) => response.json()).then(setCourse); }, [id]); if (!course) return <main className="p-8 text-slate-500">Loading quiz...</main>; return <main className="mx-auto max-w-2xl px-4 py-12"><div className="rounded-2xl border border-slate-200 bg-white p-8 shadow-soft"><p className="text-sm font-bold uppercase tracking-wider text-primary-500">Course quiz</p><h1 className="mt-2 text-3xl font-extrabold text-slate-900">What is the best way to learn?</h1><div className="mt-6 space-y-3">{['Practice regularly', 'Never review lessons', 'Skip all exercises', 'Avoid asking questions'].map((answer) => <label key={answer} className="flex cursor-pointer gap-3 rounded-xl border border-slate-200 p-4 hover:bg-slate-50"><input type="radio" name="answer" value={answer} checked={selected === answer} onChange={(event) => setSelected(event.target.value)} />{answer}</label>)}</div><button disabled={!selected} onClick={() => setSubmitted(true)} className="mt-6 rounded-xl bg-primary-500 px-5 py-3 font-bold text-white disabled:opacity-40">Submit answer</button>{submitted && <p className="mt-5 rounded-xl bg-green-50 p-4 text-green-700">{selected === 'Practice regularly' ? 'Correct! Great work.' : 'Keep practicing and try again.'}</p>}</div></main>; }
export default Quiz;
