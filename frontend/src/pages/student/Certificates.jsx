import { useEffect, useState } from 'react';
function Certificates() { const user = JSON.parse(localStorage.getItem('user') || 'null'); const [items, setItems] = useState([]); useEffect(() => { const load = async () => { if (!user?.id) return; const response = await fetch(`${import.meta.env.VITE_API_URL || 'http://localhost:5000/api'}/enrollments?student_id=${user.id}`); setItems((await response.json()).filter((item) => item.progress === 100)); }; load(); }, [user?.id]); return <main className="mx-auto max-w-5xl px-4 py-8 sm:px-6 lg:px-8"><h1 className="text-3xl font-extrabold text-slate-900">Certificates</h1>{items.length === 0 ? <p className="mt-8 rounded-2xl border border-dashed border-slate-300 p-10 text-center text-slate-500">Complete a course to earn a certificate.</p> : <div className="mt-8 grid gap-6 md:grid-cols-2">{items.map((item) => <article key={item.id} className="rounded-2xl border-4 border-primary-100 bg-white p-8 text-center shadow-soft"><p className="text-sm font-bold uppercase tracking-widest text-primary-500">Certificate of Completion</p><p className="mt-6 text-slate-500">Presented to</p><h2 className="mt-2 text-2xl font-extrabold">{user?.name}</h2><p className="mt-5 text-slate-500">For completing</p><p className="mt-2 font-bold text-slate-900">{item.course?.title}</p><button onClick={() => window.print()} className="mt-7 rounded-xl bg-primary-500 px-4 py-2 font-semibold text-white">Download certificate</button></article>)}</div>}</main>; }
export default Certificates;
