import { useEffect, useState } from 'react';
const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:5000/api';
function Dashboard() { const [stats, setStats] = useState(null); useEffect(() => { fetch(`${API_URL}/admin/stats`).then((r) => r.json()).then(setStats); }, []); return <section><h1 className="text-3xl font-extrabold text-slate-900">Admin Dashboard</h1><p className="mt-2 text-slate-500">Manage the LearnHub platform.</p><div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{[['Students', stats?.students], ['Courses', stats?.courses], ['Instructors', stats?.instructors], ['Enrollments', stats?.enrollments]].map(([label, value]) => <div key={label} className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-3xl font-extrabold text-slate-900">{value ?? '—'}</p></div>)}</div></section>; }
export default Dashboard;
