import { useEffect, useState } from 'react';
import { apiFetch } from '../../lib/api';
function Dashboard() { const [stats, setStats] = useState(null); const [error, setError] = useState(''); useEffect(() => { apiFetch('/admin/stats').then((r) => r.ok ? r.json() : Promise.reject(new Error('Unable to load admin statistics.'))).then(setStats).catch((e) => setError(e.message)); }, []); return <section><h1 className="text-3xl font-extrabold">Admin Dashboard</h1><p className="mt-2 text-slate-500">Manage the LearnHub platform.</p>{error && <p className="mt-5 rounded-xl bg-red-50 p-4 text-red-700">{error}</p>}<div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{[['Students', stats?.students], ['Courses', stats?.courses], ['Instructors', stats?.instructors], ['Enrollments', stats?.enrollments]].map(([label, value]) => <div key={label} className="rounded-2xl border bg-white p-6 shadow-sm"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-3xl font-extrabold">{value ?? '—'}</p></div>)}</div></section>; }
export default Dashboard;
