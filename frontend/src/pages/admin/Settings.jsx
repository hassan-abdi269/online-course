import { useState } from 'react';
function Settings() { const [saved, setSaved] = useState(false); const [form, setForm] = useState({ name: localStorage.getItem('site_name') || 'LearnHub', email: localStorage.getItem('site_email') || '' }); function submit(event) { event.preventDefault(); localStorage.setItem('site_name', form.name); localStorage.setItem('site_email', form.email); setSaved(true); } return <section className="max-w-2xl"><h1 className="text-3xl font-extrabold text-slate-900">Settings</h1><form onSubmit={submit} className="mt-8 space-y-5 rounded-2xl border border-slate-200 bg-white p-6"><label className="block font-semibold">Site name<input value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} className="mt-2 w-full rounded-xl border p-3 font-normal" /></label><label className="block font-semibold">Contact email<input type="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} className="mt-2 w-full rounded-xl border p-3 font-normal" /></label><button className="rounded-xl bg-primary-500 px-5 py-3 font-bold text-white">Save settings</button>{saved && <p className="text-sm text-green-600">Settings saved.</p>}</form></section>; }
export default Settings;
