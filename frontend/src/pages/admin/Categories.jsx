import { useEffect, useState } from 'react';
const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:5000/api';
function Categories() { const [items, setItems] = useState([]); const [name, setName] = useState(''); const [description, setDescription] = useState(''); const load = () => fetch(`${API_URL}/categories`).then((r) => r.json()).then(setItems); useEffect(load, []); async function add(event) { event.preventDefault(); if (!name.trim()) return; const response = await fetch(`${API_URL}/categories`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ name, description }) }); if (response.ok) { setName(''); setDescription(''); load(); } } async function remove(id) { if (window.confirm('Delete this category?') && (await fetch(`${API_URL}/categories/${id}`, { method: 'DELETE' })).ok) load(); } return <section><h1 className="text-3xl font-extrabold text-slate-900">Categories</h1><form onSubmit={add} className="mt-6 flex flex-wrap gap-3 rounded-2xl border border-slate-200 bg-white p-5"><input required value={name} onChange={(e) => setName(e.target.value)} placeholder="Category name" className="rounded-xl border p-3" /><input value={description} onChange={(e) => setDescription(e.target.value)} placeholder="Description" className="min-w-64 flex-1 rounded-xl border p-3" /><button className="rounded-xl bg-primary-500 px-5 font-semibold text-white">Add</button></form><div className="mt-6 space-y-3">{items.map((item) => <div key={item.id} className="flex items-center justify-between rounded-xl border border-slate-200 bg-white p-4"><div><p className="font-bold">{item.name}</p><p className="text-sm text-slate-500">{item.course_count} courses</p></div><button onClick={() => remove(item.id)} className="font-semibold text-red-600">Delete</button></div>)}</div></section>; }
export default Categories;
