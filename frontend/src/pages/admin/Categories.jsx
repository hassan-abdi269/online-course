import { useEffect, useState } from 'react';
import { apiFetch } from '../../lib/api';
function Categories() { const [items, setItems] = useState([]); const [name, setName] = useState(''); const [description, setDescription] = useState(''); const load = () => apiFetch('/categories').then((r) => r.json()).then(setItems); useEffect(load, []); async function add(e) { e.preventDefault(); const r = await apiFetch('/categories', { method: 'POST', body: JSON.stringify({ name, description }) }); if (r.ok) { setName(''); setDescription(''); load(); } } async function remove(id) { if (window.confirm('Delete this category?') && (await apiFetch(`/categories/${id}`, { method: 'DELETE' })).ok) load(); } return <section><h1 className="text-3xl font-extrabold">Categories</h1><form onSubmit={add} className="mt-6 flex flex-wrap gap-3 rounded-2xl border bg-white p-5"><input required value={name} onChange={(e) => setName(e.target.value)} placeholder="Category name" className="rounded-xl border p-3" /><input value={description} onChange={(e) => setDescription(e.target.value)} placeholder="Description" className="min-w-64 flex-1 rounded-xl border p-3" /><button className="rounded-xl bg-primary-500 px-5 font-semibold text-white">Add</button></form><div className="mt-6 space-y-3">{items.map((item) => <div key={item.id} className="flex items-center justify-between rounded-xl border bg-white p-4"><div><p className="font-bold">{item.name}</p><p className="text-sm text-slate-500">{item.course_count} courses</p></div><button onClick={() => remove(item.id)} className="font-semibold text-red-600">Delete</button></div>)}</div></section>; }
export default Categories;
