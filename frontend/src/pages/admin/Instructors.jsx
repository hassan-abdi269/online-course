import { useEffect, useState } from 'react';
const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:5000/api';
function Instructors() { const [items, setItems] = useState([]); useEffect(() => { fetch(`${API_URL}/instructors`).then((r) => r.json()).then(setItems); }, []); return <section><h1 className="text-3xl font-extrabold text-slate-900">Instructors</h1><div className="mt-8 grid gap-4 md:grid-cols-2">{items.map((item) => <article key={item.id} className="rounded-2xl border border-slate-200 bg-white p-5"><h2 className="font-bold text-slate-900">{item.name}</h2><p className="mt-1 text-sm text-slate-500">{item.email}</p><p className="mt-4 text-sm">{item.specialization || 'General instructor'} · {item.courses_count} courses</p></article>)}</div>{!items.length && <p className="mt-8 text-slate-500">No instructors found.</p>}</section>; }
export default Instructors;
