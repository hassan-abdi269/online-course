import { useEffect, useState } from 'react';
const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:5000/api';
function Students() { const [items, setItems] = useState([]); useEffect(() => { fetch(`${API_URL}/admin/students`).then((r) => r.json()).then(setItems); }, []); return <section><h1 className="text-3xl font-extrabold text-slate-900">Students</h1><div className="mt-8 overflow-x-auto rounded-2xl border border-slate-200 bg-white"><table className="w-full min-w-[600px] text-left text-sm"><thead className="border-b bg-slate-50"><tr><th className="p-4">Name</th><th className="p-4">Email</th><th className="p-4">Enrollments</th><th className="p-4">Joined</th></tr></thead><tbody>{items.map((item) => <tr key={item.id} className="border-b"><td className="p-4 font-semibold">{item.name}</td><td className="p-4">{item.email}</td><td className="p-4">{item.enrollments_count}</td><td className="p-4">{item.created_at ? new Date(item.created_at).toLocaleDateString() : '—'}</td></tr>)}</tbody></table>{!items.length && <p className="p-8 text-center text-slate-500">No students found.</p>}</div></section>; }
export default Students;
