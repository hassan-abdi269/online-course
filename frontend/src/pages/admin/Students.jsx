import { useEffect, useState } from 'react';
import { apiFetch } from '../../lib/api';
function Students() { const [items, setItems] = useState([]); const [error, setError] = useState(''); useEffect(() => { apiFetch('/admin/students').then((r) => r.ok ? r.json() : Promise.reject(new Error('Unable to load students.'))).then(setItems).catch((e) => setError(e.message)); }, []); return <section><h1 className="text-3xl font-extrabold">Students</h1>{error && <p className="mt-5 rounded-xl bg-red-50 p-4 text-red-700">{error}</p>}<div className="mt-8 overflow-x-auto rounded-2xl border bg-white"><table className="w-full min-w-[600px] text-left text-sm"><thead className="border-b bg-slate-50"><tr><th className="p-4">Name</th><th className="p-4">Email</th><th className="p-4">Enrollments</th><th className="p-4">Joined</th></tr></thead><tbody>{items.map((item) => <tr key={item.id} className="border-b"><td className="p-4 font-semibold">{item.name}</td><td className="p-4">{item.email}</td><td className="p-4">{item.enrollments_count}</td><td className="p-4">{item.created_at ? new Date(item.created_at).toLocaleDateString() : '—'}</td></tr>)}</tbody></table>{!items.length && <p className="p-8 text-center text-slate-500">No students found.</p>}</div></section>; }
export default Students;
